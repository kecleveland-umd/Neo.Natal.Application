@model IEnumerable<Neo_Natal.Models.HealthWorker>


@{
    ViewBag.Title = "Index";
}

<h2>HEALTHWORKER</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Username
        </th>
       
        <th>
            Email
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.username)
        </td>
        <!--<td>
            @Html.DisplayFor(modelItem => item.password)
        </td>-->
        <td>
            @Html.DisplayFor(modelItem => item.email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}
</table>

<p>@Html.ActionLink("Client List", "Index", "Clients")</p>

<!--CLIENTS
First Name: [gets populated by Client model]
Last Name:-->


    @foreach (var item in Model)
    {
        <table>
            <tr>
                @Html.Partial("_ClientInfo")
            </tr>
        </table>
    }


<p>@Html.ActionLink("Create New Client", "Create", "Clients")</p>
